# budgeting-backend
REST API for personal budgeting app with transaction tracking, budget limits, expense categorization, and monthly summaries. Built with Node.js, Express, PostgreSQL and Prisma. Includes JWT authentication and budget alerts.

# Quick Start
'''bash
npm install
cp .env.example .env
npx prisma migrate dev
npm start
